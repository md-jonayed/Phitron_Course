#include <bits/stdc++.h>
using namespace std;

int main()
{
    int testcases;
    cin >> testcases;
    while (testcases--)
    {
        int n;
        cin >> n;
        int coins[n];
        for (int i = 0; i < n; i++)
        {
            cin >> coins[i];
        }
        sort(coins, coins + n);
        vector<int> chinki;
        vector<int> minki;
        }
    return 0;
}